import React from 'react';
import styled from 'styled-components';

// Theme and styles
import colors from '../../Themes/Colors';

const PortfolioItem = ({ className }) => {
  return <div className={className}>portfolio item</div>;
};

export default PortfolioItem;
